import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Button: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Button);