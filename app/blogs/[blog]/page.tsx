import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'blogs - blog name',
};
/**
 * ==> Component
 */
const page = ({}) => {
  return (
    <>
      <h1>blog</h1>
    </>
  );
};

export default page;
