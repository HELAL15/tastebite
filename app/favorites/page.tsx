import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Tastebite - favorites',
};
/**
 * ==> Component
 */
const page = ({}) => {
  return (
    <>
      <h1>favorites</h1>
    </>
  );
};

export default page;
