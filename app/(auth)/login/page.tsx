import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Tastebite - login to your account',
};
/**
 * ==> Component
 */
const page = ({}) => {
  return <></>;
};

export default page;
