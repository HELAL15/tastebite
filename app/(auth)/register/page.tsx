import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Tastebite - register new account',
};
/**
 * ==> Component
 */
const page = ({}) => {
  return <></>;
};

export default page;
